## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:postgresql://localhost:5432/marcus_test
spring.datasource.username= marcus
spring.datasource.password= marcus

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# gcloud credentials

# GOOGLE_APPLICATION_CREDENTIALS = /Users/marcussmith/.config/gcloud/application_default_credentials.json
